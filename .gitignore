# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# 大文件：训练数据集 / 标注中间产物 / 训练输出（全在 data/training/ 下，不进 git，太大）
/data/training/
# 可下载的预训练底座权重（放 models/，不进 git，太大）
/models/

# 临时/系统
*.log
.DS_Store
Thumbs.db
